# dependencies
node_modules/

# build output
dist/
dist-ssr/
*.local

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# iOS build artefacts (the Xcode project itself IS committed)
ios/App/App/public/
ios/App/build/
ios/App/Pods/
ios/App/.swiftpm/
DerivedData/

# Copyrighted reference books — local study material, never published
references/

# built IPA (rebuild from the archive; too large and rebuildable)
appstore/*.ipa

# App Review correspondence — personal contact details; this repo is public
appstore/review-case.txt
appstore/review-delay-enquiry.txt

# Finder-duplicated Capacitor config files (identical to config.xml)
ios/App/App/config [0-9].xml
